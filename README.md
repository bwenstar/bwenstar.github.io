# Brendan Ta
- [github](https://github.com/bwenstar "github")

# bwenstar.github.io

Using github pages to host

## How to run locally

```bash
# Install Ruby (>= 2.7) and Bundler if you don’t have them
gem install bundler
bundle init
echo "gem 'jekyll'" >> Gemfile
bundle install

# Build & serve
bundle exec jekyll serve
